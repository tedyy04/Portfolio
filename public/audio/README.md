# Background audio

Put your own background music file here as:

- `background.mp3`

This project references it at `/audio/background.mp3`.

Note: Browsers often block autoplay audio. In this app, audio is enabled via the `MUSIC ON/OFF` toggle in the navbar, and will start playing after a user gesture if needed.
